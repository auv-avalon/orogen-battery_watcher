# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(battery_watcherTaskLib)
ADD_LIBRARY(${BATTERY_WATCHER_TASKLIB_NAME} SHARED 
    ${BATTERY_WATCHER_TASKLIB_SOURCES})
add_dependencies(${BATTERY_WATCHER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${BATTERY_WATCHER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${BATTERY_WATCHER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${BATTERY_WATCHER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${BATTERY_WATCHER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${BATTERY_WATCHER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${BATTERY_WATCHER_TASKLIB_HEADERS}
    DESTINATION include/orocos/battery_watcher)

